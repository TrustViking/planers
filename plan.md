# plan.md — планер: этапы, решения, проверки

Источник требований — `TZ_planer.md` (версия 0.3). Этот файл — только статус; содержание этапов, решений и проверок — в ТЗ.

## Этапы (ТЗ §11)

| Этап | Содержание | Готово, когда | Статус |
|---|---|---|---|
| 0 | Утверждение ТЗ → `CLAUDE.md` планера, `plan.md`; тренировочная форма для restreamer (§8) | файлы в репо; URL тренировочной формы в конфиге restreamer | в работе: ТЗ 0.3, `CLAUDE.md`, `plan.md` есть; тренировочная форма не создана |
| 1 | Экспортер в restreamer (§8) | `--dry-run` кладёт `plan_*.bcast`, тесты зелёные; реальный прогон присылает файл в Telegram | сделано в restreamer, коммит `7d7afeb`; доработка по §12 п.7 (тексты слота = тексты анонса) |
| 2 | Ядро планера: `package`, `config`, `state`, `output`, `pipeline`, `--dry-run`, `--status`; платформа — фейк | на реальном пакете из этапа 1 dry-run даёт корректный отчёт; тесты §10 | не начат |
| 3 | `platforms/youtube` + `google/auth` + `reconciler`, `--check`, `--auth` | тестовый канал: эфир создан, ключ в `keys.txt`; повторный запуск — «копия»; изменённое описание — «исправлено»; удалённый руками эфир — создан заново | не начат |
| 4 | `form/discovery` + `form/submitter` | ответ виден в таблице ответов тренировочной формы; обязательное поле без данных блокирует отправку | не начат |
| 5 | exe, `planer.bat`, README для владельца; переключение на боевую форму | владелец без Python проходит §4 по инструкции; перенос экспортера в broadcaster и push | не начат |
| 6 | Facebook | после закрытия **[ПРОВЕРИТЬ]** по §7.4 | не начат |

## Решения §12

| № | Решение | Статус |
|---|---|---|
| 1 | `start` в манифесте — ISO-8601 со смещением, единственное исключение из `DD-MM-YYYY` (§5.1) | принято 10-09-2026 |
| 2 | Форма: вопросы «ID слота», «Адрес потока (RTMP)», «Ссылка на эфир» (§6.1 п.2) | открыто |
| 3 | Исправление изменившегося эфира — обновление на месте, а не отмена + пересоздание (§7.3) | открыто |
| 4 | Эфиры с маркером планера, чьё время пропало из слотов, — показывать в отчёте, не удалять (§7.3) | открыто |
| 5 | В режиме `audit` броадкастера пакет строится из ветки `merge` (§8) | принято 10-09-2026 |
| 6 | В `form.fields` добавлен ключ `platform` (§5.1, §8) | принято 10-09-2026 |
| 7 | Тексты слота в пакете = тексты анонса; в пакет попадает каждая пара (слот, язык), по которой был анонс (§5.1) | принято 10-09-2026 |

## Проверки [ПРОВЕРИТЬ]

| Проверка | ТЗ | Статус |
|---|---|---|
| Лимит 100 пользователей для неверифицированного OAuth-приложения в статусе In production | §6.1 п.1 | открыто |
| Актуальная стоимость вызовов YouTube Data API (`insert`, `bind`, `thumbnails.set`, `list`) | §6.1 п.3 | открыто |
| Как YouTube хранит описание (режет ли пробелы/пустые строки) — нормализация при сравнении | §7.3 | открыто |
| Поведение `liveBroadcasts.update` (`part=snippet`: название, описание, превью) | §7.3 | открыто |
| Facebook: `live_videos`, права Page-токена, окно старта, срок жизни ключа, app review или роль tester | §7.4 | открыто |
| Структура `FB_PUBLIC_LOAD_DATA_` на реальной форме: название вопроса, `entry.NNN`, обязательность | §7.5 п.2 | открыто |
| `pageHistory` в POST для многостраничной формы | §7.5 п.4 | открыто |
| Маркер «ответ записан» в теле ответа `formResponse` | §7.5 п.5 | открыто |
